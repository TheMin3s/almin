import java.lang.reflect.*;
import java.nio.file.*;
/** Rasterises the marker sheet through the panel's own rules, so it can be looked at. */
public class Svg2Png {
    public static void main(String[] a) throws Exception {
        // No SVG rasteriser here; the sheet is checked in a browser instead.
        System.out.println("use the browser");
    }
}
